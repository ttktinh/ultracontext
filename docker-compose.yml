services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: ultracontext
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ultracontext
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../files/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ultracontext -d ultracontext"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - dokploy-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_PROVIDER: postgres
      DATABASE_URL: postgresql://ultracontext:${POSTGRES_PASSWORD}@postgres:5432/ultracontext
      ULTRACONTEXT_ADMIN_KEY: ${ULTRACONTEXT_ADMIN_KEY}
      PORT: 8787
      HOSTNAME: "0.0.0.0"         
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "8787:8787"
    networks:
      - dokploy-network

volumes:
  postgres_data:

networks:
  dokploy-network:
    external: true
